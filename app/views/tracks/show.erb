  <div class="track">
  <h2><%= @track.title %></h2> 
  <% if @track.url %>
  <h3><a href="<%= @track.url %>"><%= @track.url %></a> </h3> 
  <% end %>
  <small>by <%= @track.author %></small>
  </div>
  <br />
  <a href="/tracks" >&larr; Back to tracks</a>