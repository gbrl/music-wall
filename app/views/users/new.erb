<% if @user && @user.errors.any? %>
  <div>
    <strong>Some issues with your track, please fix them:</strong>
    <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>

<form method="post" action="/users">
  <div>
    <label>Username *</label>
    <input type="text" name="title" value="<%= @user && @user.username %>">
  </div>
  <div>
    <label>Email *</label>
    <input type="text" name="author" value="<%= @user && @user.email %>">
  </div>  
    <div>
    <label>Password *</label>
    <input type="text" name="url" value="<%= @user && @user.password %>">
  </div>
  <div>
    <input type="submit" value="Submit" class="button">
  </div>
  <small>* Required fields
</form>